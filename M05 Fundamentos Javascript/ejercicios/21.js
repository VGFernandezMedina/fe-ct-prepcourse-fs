function suma(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su suma.
  // Tu código:
  var sumaxy = x + y
  return sumaxy 
}

module.exports = suma;
